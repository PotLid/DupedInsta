# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DupedInsta' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DupedInsta

  target 'DupedInstaTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DupedInstaUITests' do
    # Pods for testing
  end
  
  # Add Parse pods and install
  pod 'Parse'
  pod 'AlamofireImage'
end
